databaseChangeLog:
  - changeSet:
      id: add-written-at-004
      author: adw8
      changes:
        - addColumn:
            tableName: journal
            schemaName: ${schemaName}
            columns:
              - column:
                  name: written_at
                  type: datetime
                  defaultValueComputed: "CURRENT_TIMESTAMP"
                  constraints:
                    nullable: false
